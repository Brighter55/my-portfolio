# Python demo backend (frontend/.gitignore already covers node_modules/dist)
backend/.env
backend/.venv/
backend/**/__pycache__/
backend/db.sqlite3
*.pyc
__pycache__/

# Claude Code local git worktrees (full working copies) — never commit these
.claude/worktrees/
